<%- model_class = PatientVisit -%>
<div class="page-header">
  <h1><%=t '.title', :default => PatientVisit %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:summary) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @patient_visits.each do |patient_visit| %>
      <tr>
        <td><%= patient_visit.id %></td>
        <td><%= patient_visit.summary %></td>
        <td><%=l patient_visit.created_at, format: :long %></td>
        <td>
          <%= link_to patient_visit_path(patient_visit), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to patient_visit_path(patient_visit), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_patient_visit_path,
            :class => 'btn btn-primary' %>
