<%- model_class = DiseaseCondition -%>
<div class="page-header">
  <h1><%=t '.title', :default => DiseaseCondition %></h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th>ID</th>
      <th><%= model_class.human_attribute_name(:icd_10) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @disease_conditions.each do |disease_condition| %>
      <tr>
        <td><%= disease_condition.id %></td>
        <td><%= disease_condition.icd_10 %></td>
        <td><%=l disease_condition.created_at, format: :long %></td>
        <td>
          <%= link_to disease_condition_path(disease_condition), :class => 'btn btn-xs', :title => "#{ t('.show', :default => t('helpers.links.show')) }" do %>
            <%= glyph 'info-sign' %>
            <%= glyph 'pencil' %>
          <%- end -%>
          <%= link_to disease_condition_path(disease_condition), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs', :title => "#{ t('.destroy', :default => t('helpers.links.destroy')) }" do %>
            <%= glyph 'remove' %>
          <%- end -%>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_disease_condition_path,
            :class => 'btn btn-primary' %>
